const BASE_URL = 'https://recruitment-api.vercel.app';

const Endpoints = {
  login: `${BASE_URL}/login`,
  getTable: `${BASE_URL}/get-table`,
  getInfo: `${BASE_URL}/get-info`,
};

export default Endpoints;
